# Raylib-OpenCV-Face-Detector
Detect faces using OpenCV and render 3D models on them using Raylib

## Build

```bash
mkdir -p build
cmake -S . -B build
cmake --build build -j
```

## Run
```bash
./build/app
```
